# Streamable HTTP Client

Client for the local Streamable HTTP MCP server.

## Prerequisite

Start server first:

```bash
python mcp_servers/streamablehttp/agent/server.py
```

## Run client

```bash
python -m mcp_server_client.streamablehttp.agent
```

## Default URL

- `http://localhost:3000/mcp`
