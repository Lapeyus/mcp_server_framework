# SSE Client

Client for the local SSE MCP server.

## Prerequisite

Start server first:

```bash
python mcp_servers/sse/filesystem/filesystem_server.py
```

## Run client

```bash
python -m mcp_server_client.sse.agent
```

## Default URL

- `http://localhost:3000/sse`
